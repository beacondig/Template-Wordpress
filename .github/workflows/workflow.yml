name: wp-deploy
on:
  push:
    branches:
      - feature-deploy-action
jobs:
  my_job:
    steps:
      - actions/checkout@v1
      - uses: webfactory/ssh-agent@v0.1.1
        with:
          ssh-private-key: "${{ secrets.SSH_PRIVATE_KEY }}"
